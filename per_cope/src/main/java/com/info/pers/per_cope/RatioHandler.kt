package com.info.pers.per_cope

import android.app.Activity
import android.content.Context
import android.webkit.WebView

interface RatioHandler {
    suspend fun e(d : String, h : String) : Info?

    fun gew(w : Context, s : String)

    suspend fun fg(geg: Activity,sdv : Info) : String

    fun ku(ew: Context) : Boolean

    suspend fun lfe(grg : Context, vx : Data)

    suspend fun b(w : Context) : Data

    suspend fun y(ge: Context, dgv : String)

    fun o(fw: WebView)

    fun rg(dgd: Class<out Activity>)

    fun ht(sg : Activity)
}